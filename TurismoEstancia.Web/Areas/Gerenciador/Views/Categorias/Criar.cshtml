@model CategoriaPontoTuristicoDto
@{
    ViewData["Title"] = "Nova categoria";
    ViewData["AreaAtiva"] = "maravilhas";
}
<div class="painel-page-header">
    <div>
        <h1 class="painel-page-title">Nova categoria</h1>
        <p class="painel-page-desc">A chave alimenta o mapa (ex.: heritage, nature, food). Ícone e cor usam o visual do protótipo.</p>
    </div>
</div>

<form class="painel-form" asp-action="Criar" method="post" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="painel-alert painel-alert--erro"></div>

    <div class="painel-form-grid">
        @await Html.PartialAsync("_SeletorChave", Model.Chave)
        <div class="painel-field">
            <label asp-for="Nome"></label>
            <input asp-for="Nome" class="painel-input" maxlength="100" placeholder="Patrimônio" />
            <span asp-validation-for="Nome" class="field-validation-error"></span>
        </div>
    </div>

    <div class="painel-field">
        <label asp-for="SubTitulo"></label>
        <input asp-for="SubTitulo" class="painel-input" maxlength="160" />
    </div>

    <div class="painel-form-grid">
        <div class="painel-field">
            <label asp-for="Cor"></label>
            <input asp-for="Cor" class="painel-input" type="color" style="height:44px;padding:4px;" />
        </div>
        @await Html.PartialAsync("_SeletorIcone", Model.Icone, new ViewDataDictionary(ViewData) { ["Placeholder"] = "landmark, tree-pine...", ["Hint"] = "Vale quando não há PNG abaixo." })
    </div>

    <div class="painel-field">
        <label>Ícone PNG (pins do mapa)</label>
        <input type="file" name="icone" accept="image/png,image/*" class="painel-input" />
        <span class="painel-hint">PNG com fundo transparente (ex.: 96×96). Aparece dentro do pin no mapa, no lugar do ícone Lucide.</span>
    </div>

    <div class="painel-form-grid">
        <div class="painel-field">
            <label asp-for="Ordem"></label>
            <input asp-for="Ordem" class="painel-input" type="number" />
        </div>
        <div class="painel-field">
            <label class="painel-checkbox">
                <input asp-for="Ativo" /> Ativo no portal
            </label>
        </div>
    </div>

    <div class="painel-form-grid">
        <div class="painel-field">
            <label class="painel-checkbox">
                <input asp-for="ApresentarEmMaravilhas" /> Exibir na seção "7 Maravilhas"
            </label>
        </div>
        <div class="painel-field">
            <label class="painel-checkbox">
                <input asp-for="ExibirNoMapa" /> Exibir no mapa interativo
            </label>
        </div>
    </div>

    <div class="painel-form-actions">
        <button type="submit" class="btn-painel btn-painel--primary"><i data-lucide="check"></i> Salvar</button>
        <a class="btn-painel btn-painel--ghost" asp-action="Index"><i data-lucide="x"></i> Cancelar</a>
    </div>
</form>
