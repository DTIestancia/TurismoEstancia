@model IReadOnlyList<RoteiroDto>
@{
    ViewData["Title"] = "Roteiros";
}
<div class="painel-page-header">
    <div>
        <h1 class="painel-page-title">Roteiros</h1>
        <p class="painel-page-desc">Roteiros turísticos exibidos em /Roteiros no portal.</p>
    </div>
    <button type="button" class="btn-painel btn-painel--primary js-abrir-criar" data-titulo="Novo roteiro" data-url="@Url.Action("Criar")">
        <i data-lucide="plus"></i> Novo roteiro
    </button>
</div>

<div class="painel-card">
    <div class="painel-table-wrap">
        <table class="painel-table">
            <thead>
                <tr>
                    <th>Imagem</th>
                    <th>Título</th>
                    <th>Descrição</th>
                    <th>Ordem</th>
                    <th>Status</th>
                    <th style="text-align:right;">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @if (item.ImagemArquivoId is long imgId)
                            {
                                <img class="painel-item-thumb" src="@Url.Content($"~/arquivo/{imgId}")" alt="" />
                            }
                        </td>
                        <td><strong>@item.Titulo</strong></td>
                        <td style="max-width:320px;">@(item.Descricao ?? "—")</td>
                        <td>@item.Ordem</td>
                        <td>
                            <span class="painel-badge @(item.Ativo ? "painel-badge--green" : "painel-badge--grey")">
                                @(item.Ativo ? "Ativo" : "Inativo")
                            </span>
                        </td>
                        <td>
                            <div class="painel-actions" style="justify-content:flex-end;">
                                <a class="btn-painel btn-painel--ghost btn-painel--xs" asp-page="/Roteiros/Detalhe" asp-route-id="@item.Id" target="_blank" title="Ver no portal"><i data-lucide="eye"></i></a>
                                <a class="btn-painel btn-painel--ghost btn-painel--xs" asp-action="Editar" asp-route-id="@item.Id"><i data-lucide="pencil"></i> Editar</a>
                                <form asp-action="Excluir" asp-route-id="@item.Id" method="post" class="js-confirm" data-confirm="Excluir o roteiro '@item.Titulo'?">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn-painel btn-painel--danger btn-painel--xs"><i data-lucide="trash-2"></i></button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    @if (Model.Count == 0)
    {
        <div class="painel-empty">Nenhum roteiro cadastrado.</div>
    }
</div>
